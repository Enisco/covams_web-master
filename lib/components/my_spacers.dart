import 'package:flutter/material.dart';

class SpacerUltraSmall extends StatelessWidget {
  const SpacerUltraSmall({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      height: size.height / 50000,
      width: 1,
    );
  }
}

class SpacerSmall extends StatelessWidget {
  const SpacerSmall({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      height: size.height / 200,
      width: 1,
    );
  }
}

class Spacer0 extends StatelessWidget {
  const Spacer0({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      height: size.height / 90,
      width: 1,
    );
  }
}

class Spacer1 extends StatelessWidget {
  const Spacer1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      height: size.height / 55,
      width: 1,
    );
  }
}

class Spacer2 extends StatelessWidget {
  const Spacer2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      height: size.height / 35,
      width: 1,
    );
  }
}

class Spacer3 extends StatelessWidget {
  const Spacer3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      height: size.height / 20,
      width: 1,
    );
  }
}

class Spacer4 extends StatelessWidget {
  const Spacer4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      height: size.height / 15,
      width: 1,
    );
  }
}

class Spacer5 extends StatelessWidget {
  const Spacer5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      height: size.height / 10,
      width: 1,
    );
  }
}

class SpacerLarge extends StatelessWidget {
  const SpacerLarge({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      height: size.height / 4,
      width: 1,
    );
  }
}

//----------------------------------------------------------------------------------------

class HSpacerUltraSmall extends StatelessWidget {
  const HSpacerUltraSmall({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      width: size.width / 500,
      height: 1,
    );
  }
}

class HSpacerSmall extends StatelessWidget {
  const HSpacerSmall({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      width: size.width / 100,
      height: 1,
    );
  }
}

class HSpacer0 extends StatelessWidget {
  const HSpacer0({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      width: size.width / 50,
      height: 1,
    );
  }
}

class HSpacer1 extends StatelessWidget {
  const HSpacer1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      width: size.width / 25,
      height: 1,
    );
  }
}

class HSpacer2 extends StatelessWidget {
  const HSpacer2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      width: size.width / 15,
      height: 1,
    );
  }
}

class HSpacer3 extends StatelessWidget {
  const HSpacer3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      width: size.width / 10,
      height: 1,
    );
  }
}

class HSpacer4 extends StatelessWidget {
  const HSpacer4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      width: size.width / 8,
      height: 1,
    );
  }
}

class HSpacer5 extends StatelessWidget {
  const HSpacer5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      width: size.width / 5,
      height: 1,
    );
  }
}

class HSpacerLarge extends StatelessWidget {
  const HSpacerLarge({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return SizedBox(
      width: size.width / 3,
      height: 1,
    );
  }
}
